cliente.xlsx
cliente.xlsx:Zone.Identifier

# Credenciales y artefactos locales de deploy a Netlify (no van al repo)
.env.netlify
.netlify/
.deploy_tmp/
.env

# Dependencias de las Netlify Functions (se instalan solas en el build)
node_modules/
deno.lock
netlify/functions/*.zip
netlify/functions/manifest.json
