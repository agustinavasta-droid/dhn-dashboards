# DHN - Cuentas Corrientes
# pip install -r requirements.txt

# Para leer PDFs (alternativa a pdftotext si no está disponible)
pypdf>=4.0.0

# Para leer cliente.xlsx (condición de pago por cliente)
openpyxl>=3.1.0
